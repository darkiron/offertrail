# Test Accounts

## Isolation tests

- `user_a_1774964292@test.com` / `password123`
- `user_b_1774964292@test.com` / `password123`

## Auth and reset checks

- `flow-check@example.com` / `password123`
- `ui-test@example.com` / `password123`
- `forgot-flow@example.com`
  Mot de passe initial: `password123`
  Mot de passe apres reset: `newpassword123`

## Duplicate-email checks

- `duplicate-test@example.com` / `password123` si le compte existe toujours
- `duplicate-test-2@example.com` / `password123` si le compte existe toujours

## Note

Ces comptes ont ete crees pour des tests manuels et techniques locaux. Ils ne doivent pas etre utilises comme comptes de demo permanents.
